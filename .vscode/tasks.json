{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cargo build",
            "command": "cargo build",
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build"
        }
    ]
}